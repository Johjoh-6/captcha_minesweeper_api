version: "2"
sql:
    - engine: "postgresql"
      queries: "sql/query"
      schema: "sql/migrations"
      gen:
          go:
              package: "db"
              out: "internal/database"
              sql_package: "pgx/v5"
